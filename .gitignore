# Dependencies
node_modules/

# Environment and secrets — never commit real keys
.env
.env.local
.env.*.local
!.env.example

# Test outputs (CI publishes these)
test-results/

# Build outputs
dist/
.next/
out/
build/
*.tsbuildinfo

# IDE and system
.cursor/
.cursor-server/
.cache/
.idea/
.vscode/
*.log
.DS_Store

# Optional
*.local

# System / user files (do not commit)
.bashrc
.profile
.viminfo
.ssh/

# Runtime / deployment (server-specific)
.pm2/
.npm/

# Figma export (large JSON; токены в figma-design-tokens.md)
design/figma-file.json
design/figma-export.json
design/figma-texts.json
design/figma-spec.json
